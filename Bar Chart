SELECT 
    nationality AS label,
    ROUND(AVG(english_grade), 2) AS english,
    ROUND(AVG(math_grade), 2) AS math,
    ROUND(AVG(sciences_grade), 2) AS sciences
FROM 
    STUDENT_DATASET
GROUP BY 
    nationality
ORDER BY 
    math DESC;
